import tsplib95
import csv

# problem = tsplib95.load("data/pliku.tsp")
#
# with open("miasta.csv", mode="w", newline="") as csv_file:
#     writer = csv.writer(csv_file)
#     writer.writerow(["CityID", "X", "Y"])  # nagłówki
#
#     for city_id, (x, y) in problem.node_coords.items():
#         writer.writerow([city_id, x, y])
print('abc')
printsd
